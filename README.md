# Project Setup and Running Instructions

## Building the Project
After completing the development process, run the following command to build and start the project:

### `npm run build`
### `npm start`

## Running the Project During Development
To run the project during development, use the following command:

### `npm run dev`